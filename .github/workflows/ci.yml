name: CI/CD
on:
   push:
     branches: ["main", "feature"]
jobs:
   build:
     runs-on: ubuntu-latest
     steps:
       - name: checkout code
         uses: actions/checkout@v3
       - name: docker tets
         run: docker --version
       - name: start
         run: docker compose build
